const navigatorOptions = {
  enableHighAccuracy: true,
  timeout: 5000,
  maximumAge: 0,
}

const defaultCoordinates = { // Minsk
  latitude: 53.902496,
  longitude: 27.561481,
};

export { navigatorOptions, defaultCoordinates };
